# Secrets / local env
.env
.env.local
*.env
!.env.example
# Local backups of env files (may contain old/corrupted secrets) — never commit
*.bak

# Pipeline run logs (contain scraped business data)
nightly.log
*.log

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Scraper output / artifacts
scrappers/google-maps-scraper/output/
scrappers/**/leads_raw.json
scrappers/**/leads.json
scrappers/**/leads.csv
webdata/
